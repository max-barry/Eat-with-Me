{
    "functions": {
        "source": "functions/dist",
        "predeploy": [
            "yarn clean && yarn build:funcs && yarn deps:copy && yarn deps:install",
            "npm --prefix \"$RESOURCE_DIR\" run lint"
        ]
    }
}
