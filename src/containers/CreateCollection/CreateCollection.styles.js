import styled from 'react-emotion';
import { bs, sFlexed } from '../../settings/styles';

export const CreateContainer = styled('div')({});

export const CreateLoadingRow = styled('div')({
    display: 'flex',
    justifyContent: 'space-around',
    padding: bs(0.5)
});

export const CreateActionContainer = styled('div')({
    paddingTop: bs(0.5)
});
