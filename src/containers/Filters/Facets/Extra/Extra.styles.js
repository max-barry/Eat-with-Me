import styled from 'react-emotion';
import { bs, dimensions } from '../../../../settings/styles';

export const ExtrasContainer = styled('div')({
    maxWidth: dimensions.filtersComponentMinWidth
});

export const ExtrasFilterWrap = styled('div')({
    marginBottom: bs()
});
