import { css } from 'react-emotion';

export const InteractiveStyle = css({
    cursor: 'pointer',
    userSelect: 'none'
});

export const COLORS = {
    primary: '',
    favourite: 'red',
    valid: 'green'
};
