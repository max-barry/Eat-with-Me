User creates a collection.

User can add other collections to their collections (like it).

User can subscribe to collections of other users.

A collection has a liker count

A user has a list of collections they like.